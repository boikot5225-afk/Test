# Copy to .env and fill in before `docker compose up`.
# Generate a token with: openssl rand -hex 32
SELFHOST_TOKEN=replace-with-a-long-random-secret

# base = fits comfortably in 2GB RAM, decent quality. small = better
# quality, needs more RAM/CPU time — try it only if base isn't good enough.
WHISPER_MODEL_SIZE=base
