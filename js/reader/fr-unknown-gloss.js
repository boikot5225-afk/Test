// French Unknown glosses for toc120. Immediate translation is bundled FR->RU WikDict.
const MODE_KEY='an2_reader_fr_unknown_gloss_mode_v1',CACHE_BASE_KEY='an2_reader_fr_unknown_gloss_cache_v1',MAX_CACHE=8000,MAX_BATCH=48,PREFETCH_PAGES=2;
const DICT_URL=new URL('../../../frreader/fr_ru_core.json?v=1',import.meta.url).href;
let scanTimer=null,rootObserver=null,rootObserved=null,viewObserver=null,viewObserved=null,dictionaryPromise=null,dictionary=null,lookupInFlight=false;
function scopedKey(base){try{return globalThis.an2ReaderStorageKey?.(base)||base;}catch{return base;}}
function currentLang(){const raw=String(document.getElementById('reader-reading-view')?.dataset?.readerLang||document.getElementById('reader-chapter-text')?.dataset?.lang||'').trim().toLowerCase();return raw==='french'||raw==='fr'||raw.startsWith('fr-')?'fr':raw;}
function mode(){try{return localStorage.getItem(MODE_KEY)==='off'?'off':'unknown';}catch{return'unknown';}}function enabled(){return mode()==='unknown';}
function readJson(key){try{const value=JSON.parse(localStorage.getItem(key)||'{}');return value&&typeof value==='object'&&!Array.isArray(value)?value:{};}catch{return{};}}function writeJson(key,value){try{localStorage.setItem(key,JSON.stringify(value||{}));return true;}catch{return false;}}
function normalizeSurface(word){return String(word||'').normalize('NFC').replace(/[’‘`´]/g,"'").replace(/[‐‑‒–—]/g,'-').trim().toLocaleLowerCase('fr-FR');}
function containsCyrillic(value){return/[\u0400-\u052f]/.test(String(value||''));}
function compactRussian(value){const full=String(value||'').replace(/\s+/g,' ').trim();if(!full||!containsCyrillic(full))return'';const first=full.split(/\s*\|\s*|\s*[;；]\s*|\s*\/\s*/).filter(Boolean)[0]||full;if(first.length<=34)return first;const words=first.split(/\s+/).filter(Boolean);let out='';for(const word of words){const next=out?`${out} ${word}`:word;if(next.length>34)break;out=next;}return out||first.slice(0,34).trim();}
function glossFontSize(surface,ru){const a=Math.max(2,Array.from(String(surface||'')).length),b=Math.max(1,Array.from(String(ru||'')).length),ratio=b/a,em=Math.max(.27,Math.min(.46,.47/Math.sqrt(Math.max(1,ratio))));return`${em.toFixed(3)}em`;}
function normalizedKey(word){return normalizeSurface(word);}function ownCache(){return readJson(scopedKey(CACHE_BASE_KEY));}function saveOwnCache(cache){let entries=Object.entries(cache||{});if(entries.length>MAX_CACHE){entries.sort((a,b)=>Number(b[1]?.t||0)-Number(a[1]?.t||0));entries=entries.slice(0,MAX_CACHE);}writeJson(scopedKey(CACHE_BASE_KEY),Object.fromEntries(entries));}
function lemmaFor(word){try{return normalizeSurface(globalThis.readerFrenchLemmaFor?.(word)||word);}catch{return normalizeSurface(word);}}
function isFrenchWord(el){return!!el?.classList?.contains('reader-word')&&/[A-Za-zÀ-ÖØ-öø-ÿŒœÆæÇç]/u.test(String(el.dataset?.word||el.textContent||''));}
function knowledge(el){if(el?.classList?.contains('rw-migaku-unknown'))return'unknown';if(el?.classList?.contains('rw-migaku-known'))return'known';return'';}
function wrapperFor(el){return el?.parentElement?.classList?.contains('rw-fr-gloss-wrap')?el.parentElement:null;}
function glossNode(wrap){let node=wrap?.querySelector?.(':scope > .rw-fr-gloss-text');if(!node&&wrap){node=document.createElement('span');node.className='rw-fr-gloss-text';node.setAttribute('aria-hidden','true');wrap.appendChild(node);}return node;}
function syncVisibility(el,wrap){const state=knowledge(el);wrap.dataset.frGlossVisible=state==='unknown'?'1':'0';return state;}
function hasContextOverride(wrap){return!!String(wrap?.dataset?.frContextProvider||'').trim();}
function setWrapperTranslation(wrap,word,value,options={}){const ru=compactRussian(value),node=glossNode(wrap);if(!node)return'';if(!ru)return String(node.textContent||'').trim();if(!options.force&&hasContextOverride(wrap))return String(node.textContent||'').trim();node.textContent=ru;wrap.dataset.frGlossRu=ru;wrap.style.setProperty('--fr-gloss-font',glossFontSize(word,ru));return ru;}
function ensureWrapper(el,ru=''){if(!isFrenchWord(el))return null;const word=String(el.dataset.word||el.textContent||'').trim();if(!word)return null;let wrap=wrapperFor(el);if(!wrap){wrap=document.createElement('span');wrap.className='rw-fr-gloss-wrap';wrap.dataset.frGloss='1';el.parentNode?.insertBefore(wrap,el);wrap.appendChild(el);}glossNode(wrap);if(ru)setWrapperTranslation(wrap,word,ru);syncVisibility(el,wrap);return wrap;}
function rememberTranslation(sourceWord,ru,provider='wikdict_fr_ru_offline'){const translation=compactRussian(ru),key=normalizedKey(sourceWord);if(!key||!translation)return false;const cache=ownCache();cache[key]={ru:translation,t:Date.now(),provider};saveOwnCache(cache);return true;}
function bestHint(word,lemma,caches={}){const own=caches.own||{},direct=own[normalizedKey(lemma)]||own[normalizedKey(word)]||null;return compactRussian(typeof direct==='string'?direct:direct?.ru||'');}
async function loadDictionary(){if(dictionary)return dictionary;if(dictionaryPromise)return dictionaryPromise;dictionaryPromise=fetch(DICT_URL,{cache:'force-cache'}).then(response=>{if(!response.ok)throw new Error(`FR→RU dictionary HTTP ${response.status}`);return response.json();}).then(data=>{if(!data||typeof data!=='object'||Array.isArray(data))throw new Error('FR→RU dictionary payload is invalid');dictionary=data;return data;}).finally(()=>{dictionaryPromise=null;});return dictionaryPromise;}
async function dictionaryLookup(words){const dict=await loadDictionary(),out={};for(const sourceWord of[...new Set((words||[]).map(normalizeSurface).filter(Boolean))].slice(0,MAX_BATCH)){const ru=compactRussian(dict[sourceWord]||'');if(!ru)continue;out[sourceWord]=ru;rememberTranslation(sourceWord,ru);}return out;}
function injectStyles(){if(document.getElementById('rd-fr-unknown-gloss-style-v1'))return;const style=document.createElement('style');style.id='rd-fr-unknown-gloss-style-v1';style.textContent=`#reader-reading-view.rd-fr-unknown-gloss .reader-paragraph-text{line-height:1.86!important}#reader-reading-view.rd-fr-unknown-gloss .rw-fr-gloss-wrap{display:inline-block!important;vertical-align:-.36em!important;line-height:1!important;margin:0 .025em!important;padding:0 0 .56em!important;position:relative!important;overflow:visible!important;white-space:nowrap!important}#reader-reading-view.rd-fr-unknown-gloss .rw-fr-gloss-wrap>.reader-word{display:inline!important;margin:0!important;padding:0 1px!important;line-height:1.04!important;white-space:nowrap!important;word-break:keep-all!important;overflow-wrap:normal!important}#reader-reading-view.rd-fr-unknown-gloss .rw-fr-gloss-text{display:none;position:absolute!important;left:50%!important;bottom:0!important;transform:translateX(-50%)!important;max-width:none!important;white-space:nowrap!important;pointer-events:none!important;font-family:'IBM Plex Sans',sans-serif!important;font-size:var(--fr-gloss-font,.38em)!important;font-weight:400!important;line-height:1!important;color:var(--text-muted)!important;text-decoration:none!important}#reader-reading-view.rd-fr-unknown-gloss .rw-fr-gloss-wrap[data-fr-gloss-visible="1"]>.rw-fr-gloss-text:not(:empty){display:block!important}`;document.head.appendChild(style);}
function ensureControl(){const panel=document.getElementById('rd-display-panel');if(!panel)return null;let row=document.getElementById('rd-dp-fr-unknown-gloss-row');if(!row){row=document.createElement('div');row.id='rd-dp-fr-unknown-gloss-row';row.className='rd-dp-row';row.style.display='none';row.innerHTML=`<span class="rd-dp-label">French · Unknown words</span><div class="rd-dp-pills"><button type="button" class="rd-dp-pill rd-fr-gloss-mode" data-mode="off">Обычный текст</button><button type="button" class="rd-dp-pill rd-fr-gloss-mode" data-mode="unknown">Русский под Unknown</button></div>`;row.querySelectorAll('.rd-fr-gloss-mode').forEach(button=>button.addEventListener('click',()=>setMode(button.dataset.mode)));panel.appendChild(row);}return row;}
function syncControl(){const row=ensureControl(),view=document.getElementById('reader-reading-view');if(!view)return;const isFr=currentLang()==='fr';if(row){row.style.display=isFr?'flex':'none';row.querySelectorAll('.rd-fr-gloss-mode').forEach(button=>button.classList.toggle('rd-dp-active',button.dataset.mode===mode()));}view.classList.toggle('rd-fr-unknown-gloss',isFr&&enabled());}
function setMode(next){try{localStorage.setItem(MODE_KEY,next==='off'?'off':'unknown');}catch{}syncControl();scheduleScan(0);}function cachesSnapshot(){return{own:ownCache()};}
function prepareStableSlots(root=document.getElementById('reader-chapter-text')){injectStyles();syncControl();if(!enabled()||currentLang()!=='fr'||!root)return 0;const caches=cachesSnapshot();let count=0;for(const el of root.querySelectorAll('.reader-word[data-word]')){if(!isFrenchWord(el))continue;const word=String(el.dataset.word||el.textContent||'').trim(),lemma=lemmaFor(word),ru=bestHint(word,lemma,caches),wrap=ensureWrapper(el,ru);if(!wrap)continue;if(ru&&knowledge(el)==='unknown')wrap.dataset.frGlossVisible='1';count+=1;}return count;}
function isVisibleWord(el){try{const r=el.getBoundingClientRect(),h=Math.max(document.documentElement?.clientHeight||0,window.innerHeight||0);return r.width>0&&r.height>0&&r.bottom>=-80&&r.top<=h+80;}catch{return true;}}
function priorityScopes(root){const pages=Array.from(root.querySelectorAll(':scope > .rd-page'));if(!pages.length)return[{root,visibleOnly:true}];let current=pages.findIndex(page=>page.classList.contains('rd-page-current'));if(current<0)current=pages.findIndex(page=>page.classList.contains('rd-page-show'));if(current<0)current=0;return pages.slice(current,current+PREFETCH_PAGES+1).map(page=>({root:page,visibleOnly:false}));}
async function lookupMissing(tokens){if(lookupInFlight||!tokens.length)return;lookupInFlight=true;try{await dictionaryLookup(tokens.slice(0,MAX_BATCH));}catch(error){console.warn('[fr unknown gloss] bundled dictionary lookup failed',error?.message||error);}finally{lookupInFlight=false;scheduleScan(20);}}
function scan(){syncControl();if(!enabled()||currentLang()!=='fr')return;const root=document.getElementById('reader-chapter-text');if(!root)return;prepareStableSlots(root);const caches=cachesSnapshot(),missing=[],seen=new Set();for(const scope of priorityScopes(root)){for(const el of scope.root.querySelectorAll('.reader-word[data-word]')){if(!isFrenchWord(el)||knowledge(el)!=='unknown')continue;if(scope.visibleOnly&&!isVisibleWord(el))continue;const word=String(el.dataset.word||el.textContent||'').trim(),lemma=lemmaFor(word),ru=bestHint(word,lemma,caches),wrap=ensureWrapper(el,ru);if(ru){if(wrap)wrap.dataset.frGlossVisible='1';continue;}for(const token of[normalizeSurface(lemma||word),normalizeSurface(word)]){if(!token||seen.has(token))continue;seen.add(token);missing.push(token);if(missing.length>=MAX_BATCH)break;}if(missing.length>=MAX_BATCH)break;}if(missing.length>=MAX_BATCH)break;}if(missing.length)void lookupMissing(missing);}
function scheduleScan(delay=35){clearTimeout(scanTimer);scanTimer=setTimeout(scan,Math.max(0,Number(delay)||0));}function scanNow(){scheduleScan(0);}
function bindObservers(){if(typeof MutationObserver==='undefined')return;const root=document.getElementById('reader-chapter-text');if(root&&root!==rootObserved){rootObserver?.disconnect();rootObserved=root;rootObserver=new MutationObserver(()=>scheduleScan(45));rootObserver.observe(root,{childList:true,subtree:true,attributes:true,attributeFilter:['class','data-lang','data-word']});}const view=document.getElementById('reader-reading-view');if(view&&view!==viewObserved){viewObserver?.disconnect();viewObserved=view;viewObserver=new MutationObserver(()=>{syncControl();scheduleScan(35);});viewObserver.observe(view,{attributes:true,attributeFilter:['data-reader-lang','class']});}}
function boot(){if(typeof document==='undefined'||typeof document.createElement!=='function')return;injectStyles();ensureControl();syncControl();bindObservers();scheduleScan(0);}
if(typeof window!=='undefined'&&!window.__readerFrUnknownGlossV1Installed){window.__readerFrUnknownGlossV1Installed=true;window.readerSetFrUnknownGlossMode=setMode;window.readerGetFrUnknownGlossMode=mode;window.readerPrepareFrStableSlots=prepareStableSlots;window.readerPrefetchFrUnknownGloss=scanNow;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.addEventListener('pageshow',()=>{boot();scheduleScan(25);});window.addEventListener('reader:fr-vocab-ready',()=>scheduleScan(0));window.addEventListener('an2:languagechange',()=>scheduleScan(0));}
export{mode,enabled,compactRussian,prepareStableSlots};
