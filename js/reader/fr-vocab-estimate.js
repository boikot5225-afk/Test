import { wordStateIdbPut } from './word-state-idb-store.js?v=1';

// French vocabulary layer for toc120. It mirrors the stable English reader:
// frequency-ranked lemmas -> conservative Known/Unknown -> manual override.
// Morphology is generated from wordhoard's French forms at build time.
const PROFILE_BASE_KEY='an2_reader_vocab_estimate_fr_v1',WORD_STATE_BASE_KEY='an2_reader_word_state_v1',OWNER_KEY='an2_reader_active_owner_v1';
const FREQ_URL=new URL('../../../frreader/fr_vocab_frequency.tsv?v=1',import.meta.url).href;
const LEMMA_URL=new URL('../../../frreader/fr_vocab_lemma.tsv?v=1',import.meta.url).href;
const MODAL_ID='reader-vocab-estimate-modal',WORDS_PER_PAGE=14,STEP1_COUNT=42,STEP2_COUNT=42,MIN_EXPECTED_COUNT=50000;
let dataPromise=null,frenchData=null,assessment=null,renderObserver=null,renderObserverRoot=null,viewObserver=null,viewObserverRoot=null;
let pendingWordNodes=new Set(),pendingBatchScheduled=false;
function canonicalLang(value){const lang=String(value||'').trim().toLowerCase();if(lang==='fr'||lang==='french'||lang.startsWith('fr-'))return'fr';if(lang==='en'||lang==='english'||lang.startsWith('en-'))return'en';if(lang.startsWith('zh')||lang==='cn')return'zh';if(lang.startsWith('ja')||lang==='jp')return'ja';if(lang.startsWith('es'))return'es';return lang||'fr';}
function currentLang(){return canonicalLang(document.getElementById('reader-reading-view')?.dataset?.readerLang||document.getElementById('reader-chapter-text')?.dataset?.lang||'');}
function ownerId(){try{const stored=localStorage.getItem(OWNER_KEY);if(stored)return stored;if(localStorage.getItem('an2_guest')==='1')return'guest';}catch{}return'anon';}
function scopedKey(base){return`${base}::${ownerId()}`;}
function formatNumber(value){try{return Math.round(Number(value||0)).toLocaleString('ru-RU');}catch{return String(Math.round(Number(value||0)));}}
function normalizeSurface(value){return String(value||'').normalize('NFC').replace(/[’‘`´]/g,"'").replace(/[‐‑‒–—]/g,'-').trim().toLocaleLowerCase('fr-FR');}
function escapeHtml(value){return String(value||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');}
function elisionTail(surface){const match=normalizeSurface(surface).match(/^(?:[cdjlmnst]|qu)'(.+)$/u);return match?.[1]||'';}
function wordStateStore(){try{const live=globalThis.an2ReaderWordStateSnapshot?.();if(live&&typeof live==='object')return live;}catch{}try{return JSON.parse(localStorage.getItem(scopedKey(WORD_STATE_BASE_KEY))||'{}')||{};}catch{return{};}}
function persistWordState(store){const key=scopedKey(WORD_STATE_BASE_KEY);try{localStorage.setItem(key,JSON.stringify(store||{}));}catch{}wordStateIdbPut(key,store||{}).catch(error=>console.warn('[reader fr vocab] IndexedDB save failed',error?.message||error));}
function conservativeCountForEstimate(estimate,listLength){const value=Number(estimate||0);const count=value<20000?Math.round((value*.6)/10)*10:10000;return Math.max(0,Math.min(10000,Math.min(Math.round(count),Math.max(0,Number(listLength||0)))));}
function loadProfile(){try{const raw=JSON.parse(localStorage.getItem(scopedKey(PROFILE_BASE_KEY))||'null');if(!raw||typeof raw!=='object')return null;const estimate=Math.max(0,Math.round(Number(raw.estimate||0)));const conservativeKnownCount=Math.max(0,Math.round(Number(raw.conservativeKnownCount??conservativeCountForEstimate(estimate,raw.listLength||MIN_EXPECTED_COUNT))));return{...raw,language:'fr',estimate,conservativeKnownCount};}catch{return null;}}
function saveProfile(profile){const estimate=Math.max(0,Math.round(Number(profile?.estimate||0)));const listLength=Math.max(0,Number(profile?.listLength||frenchData?.entries?.length||MIN_EXPECTED_COUNT));const next={language:'fr',version:1,...profile,estimate,listLength,conservativeKnownCount:Math.max(0,Math.round(Number(profile?.conservativeKnownCount??conservativeCountForEstimate(estimate,listLength)))),updatedAt:new Date().toISOString()};try{localStorage.setItem(scopedKey(PROFILE_BASE_KEY),JSON.stringify(next));}catch{}return next;}
function buildFrenchData(freqText,lemmaText){const entries=[],rankExact=new Map(),rankFold=new Map();for(const rawLine of String(freqText||'').split(/\r?\n/)){if(!rawLine)continue;const tab=rawLine.indexOf('\t'),word=(tab>=0?rawLine.slice(0,tab):rawLine).trim(),pos=tab>=0?rawLine.slice(tab+1).trim():'';if(!word)continue;const index=entries.length;entries.push({word,pos});if(!rankExact.has(word))rankExact.set(word,index);const folded=normalizeSurface(word);if(!rankFold.has(folded))rankFold.set(folded,{index,word});}const lemma=new Map();for(const rawLine of String(lemmaText||'').split(/\r?\n/)){if(!rawLine)continue;const tab=rawLine.indexOf('\t');if(tab<=0)continue;const surface=normalizeSurface(rawLine.slice(0,tab)),target=normalizeSurface(rawLine.slice(tab+1));if(surface&&target)lemma.set(surface,target);}return{entries,rankExact,rankFold,lemma};}
async function loadFrenchData(){if(frenchData)return frenchData;if(dataPromise)return dataPromise;dataPromise=Promise.all([fetch(FREQ_URL,{cache:'force-cache'}).then(r=>{if(!r.ok)throw new Error(`French frequency: HTTP ${r.status}`);return r.text();}),fetch(LEMMA_URL,{cache:'force-cache'}).then(r=>{if(!r.ok)throw new Error(`French morphology: HTTP ${r.status}`);return r.text();})]).then(([freqText,lemmaText])=>{const data=buildFrenchData(freqText,lemmaText);if(data.entries.length<MIN_EXPECTED_COUNT)throw new Error(`French frequency list too small: ${data.entries.length}`);if(data.rankFold.size!==data.entries.length)throw new Error(`French frequency list has ${data.entries.length-data.rankFold.size} duplicate lemmas`);for(const[index,expected]of[[0,'le'],[1,'être'],[2,'de'],[3,'un'],[4,'je']])if(normalizeSurface(data.entries[index]?.word)!==expected)throw new Error(`French frequency mismatch #${index+1}: ${data.entries[index]?.word||'∅'} != ${expected}`);for(const[surface,expected]of Object.entries({est:'être',suis:'être',étaient:'être',ai:'avoir',avait:'avoir'}))if(data.lemma.get(surface)!==expected)throw new Error(`French morphology mismatch: ${surface} -> ${data.lemma.get(surface)||'∅'} != ${expected}`);frenchData=data;try{window.dispatchEvent(new CustomEvent('reader:fr-vocab-ready'));}catch{}return data;}).finally(()=>{dataPromise=null;});return dataPromise;}
function lemmaForWordSync(word){const raw=normalizeSurface(word);if(!raw)return'';if(!frenchData)return raw;const mapped=frenchData.lemma.get(raw);if(mapped)return mapped;const ranked=frenchData.rankFold.get(raw);if(ranked?.word)return ranked.word;const tail=elisionTail(raw);if(tail){const tailMapped=frenchData.lemma.get(tail);if(tailMapped)return tailMapped;const tailRanked=frenchData.rankFold.get(tail);if(tailRanked?.word)return tailRanked.word;}return raw;}
function rankIndexForWordSync(word){if(!frenchData)return null;const hit=frenchData.rankFold.get(normalizeSurface(lemmaForWordSync(word)));return Number.isInteger(hit?.index)?hit.index:null;}
function manualKnowledge(state){const explicit=String(state?.manualKnowledge||'').toLowerCase();if(explicit==='known'||explicit==='unknown')return explicit;const status=String(state?.status||'').trim().toLowerCase();if(state?.known===true&&status==='known'&&state?.autoKnown===false)return'known';if(state?.known===false&&state?.saved===true&&(status==='problem'||status==='hard'))return'unknown';return'';}
function directStateKey(word){return`fr:${normalizeSurface(word)}`;}
function findWordState(word,create=false){const raw=String(word||'').trim(),canonical=lemmaForWordSync(raw)||normalizeSurface(raw),store=wordStateStore();if(!canonical)return{store,key:'',state:null,canonical:''};const key=directStateKey(canonical);if(store[key])return{store,key,state:store[key],canonical};const rawKey=directStateKey(raw);if(store[rawKey])return{store,key:rawKey,state:store[rawKey],canonical};for(const[candidateKey,state]of Object.entries(store)){if(!state||canonicalLang(state.lang)!=='fr')continue;if(lemmaForWordSync(state.word)===canonical)return{store,key:candidateKey,state,canonical};}if(!create)return{store,key,state:null,canonical};store[key]={word:canonical,lang:'fr',seen:0,clicked:0,saved:false,known:false,status:'new',places:{},clickContexts:{},updatedAt:new Date().toISOString()};return{store,key,state:store[key],canonical};}
function manualKnowledgeMapSnapshot(store=wordStateStore()){const latest=new Map();for(const state of Object.values(store||{})){if(!state||canonicalLang(state.lang)!=='fr')continue;const explicit=manualKnowledge(state);if(!explicit)continue;const canonical=lemmaForWordSync(state.word);if(!canonical)continue;const stamp=Date.parse(state.updatedAt||'')||0,prev=latest.get(canonical);if(!prev||stamp>=prev.stamp)latest.set(canonical,{value:explicit,stamp});}return new Map(Array.from(latest,([word,info])=>[word,info.value]));}
function classificationForSnapshot(word,profile,manualMap){const canonical=lemmaForWordSync(word);if(!canonical)return{value:'',source:'',lemma:'',index:null,rank:null};const manual=manualMap?.get(canonical)||'',index=rankIndexForWordSync(canonical),rank=Number.isInteger(index)?index+1:null;if(manual)return{value:manual,source:'manual',lemma:canonical,index,rank};if(!profile)return{value:'',source:'',lemma:canonical,index,rank};if(!frenchData)return{value:'',source:'pending',lemma:canonical,index:null,rank:null};if(!Number.isInteger(index))return{value:'unknown',source:'unranked',lemma:canonical,index:null,rank:null,...profile};return{value:index<profile.conservativeKnownCount?'known':'unknown',source:'assessment',lemma:canonical,index,rank,estimate:profile.estimate,conservativeKnownCount:profile.conservativeKnownCount};}
function classificationFor(word){const{state}=findWordState(word,false),explicit=manualKnowledge(state),canonical=lemmaForWordSync(word),index=rankIndexForWordSync(canonical),rank=Number.isInteger(index)?index+1:null;if(explicit)return{value:explicit,source:'manual',state,lemma:canonical,index,rank};return classificationForSnapshot(word,loadProfile(),manualKnowledgeMapSnapshot());}
function removeKnowledgeClasses(el){el.classList.remove('rw-migaku-known','rw-migaku-unknown');delete el.dataset.readerEstimatedKnowledge;delete el.dataset.readerManualKnowledge;}
function applyClassificationToElement(el,info){removeKnowledgeClasses(el);if(info?.value==='known')el.classList.add('rw-migaku-known');else if(info?.value==='unknown'){if(info.source!=='manual'&&el.classList.contains('rw-known')){el.classList.add('rw-migaku-known');return;}if(info.source==='manual')el.classList.remove('rw-known');el.classList.add('rw-migaku-unknown');}else return;if(info.source==='manual')el.dataset.readerManualKnowledge=info.value;else el.dataset.readerEstimatedKnowledge=info.value;const surface=normalizeSurface(el.dataset.word||el.textContent||''),lemmaText=info.lemma&&surface!==normalizeSurface(info.lemma)?` · ${info.lemma}`:'',rankText=Number.isInteger(info.rank)?` · частотность #${formatNumber(info.rank)}`:'';el.title=`${info.value==='known'?'Known':'Unknown'}${lemmaText}${rankText}`;}
function applyClassificationBatch(elements){const list=Array.from(elements||[]);if(!list.length||currentLang()!=='fr')return;const profile=loadProfile(),manualMap=manualKnowledgeMapSnapshot();for(const el of list){if(!el?.classList?.contains('reader-word'))continue;if(canonicalLang(el.dataset.lang||currentLang())!=='fr')continue;applyClassificationToElement(el,classificationForSnapshot(el.dataset.word||el.textContent||'',profile,manualMap));}}
async function applyEstimateToRenderedWords(){if(currentLang()!=='fr')return;const root=document.getElementById('reader-chapter-text');if(!root)return;try{await loadFrenchData();}catch(error){console.warn('[reader fr vocab] data unavailable',error?.message||error);return;}applyClassificationBatch(root.querySelectorAll('.reader-word[data-word]'));decorateWordPanel();syncPanelKnowledge();ensureVocabularyButton();}
function currentPanelWord(){return String(document.getElementById('reader-word-title')?.textContent||'').trim();}
function showToast(message){const toast=document.getElementById('toast');if(!toast)return;toast.textContent=message;toast.style.display='block';clearTimeout(showToast._timer);showToast._timer=setTimeout(()=>{toast.style.display='none';},1700);}
function profileButtonText(){const profile=loadProfile();return profile?`≈ ${formatNumber(profile.estimate)} слов · переоценить`:'≈ Оценить словарный запас';}
function syncPanelKnowledge(){if(currentLang()!=='fr')return;const panel=document.getElementById('reader-word-panel');if(!panel)return;const yes=panel.querySelector('#reader-fr-known-btn'),no=panel.querySelector('#reader-fr-unknown-btn'),source=panel.querySelector('#reader-fr-knowledge-source'),estimateButton=panel.querySelector('#reader-fr-vocab-estimate-btn');if(estimateButton)estimateButton.textContent=profileButtonText();if(!yes||!no||!source)return;yes.classList.remove('is-active');no.classList.remove('is-active');const word=currentPanelWord();if(!word||word==='—'){source.textContent='';return;}const info=classificationFor(word);if(info.value==='known')yes.classList.add('is-active');if(info.value==='unknown')no.classList.add('is-active');const lemmaText=info.lemma&&normalizeSurface(word)!==normalizeSurface(info.lemma)?` · лемма ${info.lemma}`:'',rankText=Number.isInteger(info.rank)?` · частотность #${formatNumber(info.rank)}`:'';if(info.source==='manual')source.textContent=`${info.value==='known'?'Знаю':'Не знаю'} · вручную${lemmaText}${rankText}`;else if(info.source==='assessment')source.textContent=info.value==='known'?`По тесту: Known · базовые ${formatNumber(info.conservativeKnownCount)} из ≈ ${formatNumber(info.estimate)}${lemmaText}${rankText}`:`По тесту: Unknown · оценка ≈ ${formatNumber(info.estimate)}${lemmaText}${rankText}`;else if(info.source==='unranked')source.textContent=`Нет в частотном списке · считаю Unknown${lemmaText}`;else source.textContent=`Пройди оценку словаря или задай статус вручную${lemmaText}${rankText}`;}
function decorateWordPanel(){if(currentLang()!=='fr')return;const panel=document.getElementById('reader-word-panel'),actions=panel?.querySelector('.reader-word-actions');if(!panel||!actions)return;if(panel.dataset.migakuKnowledge!=='fr1'){panel.dataset.migakuKnowledge='fr1';panel.querySelector('.rwp-migaku-knowledge')?.remove();for(const button of actions.querySelectorAll('button')){const onclick=button.getAttribute('onclick')||'';if(onclick.includes('readerMarkSelectedWordKnown')||onclick.includes('readerMarkSelectedWordProblem')){button.style.display='none';button.setAttribute('aria-hidden','true');}}const block=document.createElement('div');block.className='rwp-migaku-knowledge';block.innerHTML=`<div class="rwp-migaku-row"><button id="reader-fr-unknown-btn" class="rwp-migaku-btn rwp-migaku-unknown" type="button">Не знаю</button><button id="reader-fr-known-btn" class="rwp-migaku-btn rwp-migaku-known" type="button">Знаю</button></div><div id="reader-fr-knowledge-source" class="rwp-migaku-source"></div><button id="reader-fr-vocab-estimate-btn" class="rwp-vocab-estimate-btn" type="button"></button>`;actions.before(block);block.querySelector('#reader-fr-known-btn')?.addEventListener('click',()=>markCurrentWord(true));block.querySelector('#reader-fr-unknown-btn')?.addEventListener('click',()=>markCurrentWord(false));block.querySelector('#reader-fr-vocab-estimate-btn')?.addEventListener('click',openVocabularyDashboard);}syncPanelKnowledge();}
async function markCurrentWord(known){if(currentLang()!=='fr')return;const word=currentPanelWord();if(!word||word==='—')return;try{await loadFrenchData();}catch{}const found=findWordState(word,true),state=found.state;if(!state)return;state.word=found.canonical||state.word||word;state.lang='fr';state.manualKnowledge=known?'known':'unknown';state.known=!!known;state.autoKnown=false;state.status=known?'known':'learning';state.updatedAt=new Date().toISOString();persistWordState(found.store);const root=document.getElementById('reader-chapter-text');root?.querySelectorAll('.reader-word[data-word]').forEach(el=>{if(canonicalLang(el.dataset.lang||currentLang())!=='fr')return;if(lemmaForWordSync(el.dataset.word||'')!==found.canonical)return;applyClassificationToElement(el,classificationFor(el.dataset.word||''));});syncPanelKnowledge();try{window.dispatchEvent(new CustomEvent('reader:fr-vocab-ready'));}catch{}showToast(known?'✓ Знаю':'Не знаю');}
function randomNormal(mean,stdDev){const u=Math.random()||Number.MIN_VALUE,v=Math.random();return Math.sqrt(-2*Math.log(u))*Math.cos(2*Math.PI*v)*stdDev+mean;}
function selectStep1Indices(length,count){const picked=[],target=Math.min(count,length);while(picked.length<target){const index=Math.floor(Math.pow(Math.random(),2)*length);if(!picked.includes(index))picked.push(index);}return picked;}
function selectStep2Indices(length,estimate,count){let center=Number(estimate||0);if(center<100)center=100;const picked=[],target=Math.min(count,length);let guard=0;while(picked.length<target&&guard<100000){guard+=1;const index=Math.floor(randomNormal(center,center*.35));if(index<0||index>=length||picked.includes(index))continue;picked.push(index);}for(let i=0;picked.length<target&&i<length;i+=1)if(!picked.includes(i))picked.push(i);return picked;}
function estimateFromChecks(checks,indices){if(!Array.isArray(checks)||!checks.length||!Array.isArray(indices)||!indices.length)return 0;const unknownPrefix=[0];for(let i=0;i<checks.length;i+=1)unknownPrefix.push(unknownPrefix[i]+(checks[i]?0:1));const knownSuffix=new Array(checks.length+1).fill(0);for(let i=checks.length-1;i>=0;i-=1)knownSuffix[i]=knownSuffix[i+1]+(checks[i]?1:0);let boundary=0;for(let i=0;i<indices.length+1;i+=1)if(unknownPrefix[i]===knownSuffix[i]){boundary=i;break;}if(boundary>=indices.length)boundary=indices.length-1;if(boundary<0)boundary=0;return boundary===0?.5*indices[boundary]:.5*(indices[boundary]+indices[boundary-1]);}
function checksFor(indices,knownSet){return indices.map(index=>knownSet.has(index));}
function ensureModal(){let modal=document.getElementById(MODAL_ID);if(modal)return modal;modal=document.createElement('div');modal.id=MODAL_ID;modal.addEventListener('click',event=>{if(event.target===modal)closeVocabularyEstimate();});document.body.appendChild(modal);return modal;}
function modalShell(inner,{title='Оценка словарного запаса',back=false}={}){return`<div class="rve-card"><div class="rve-head">${back?'<button class="rve-back" type="button" aria-label="Назад">←</button>':'<span class="rve-head-spacer"></span>'}<div class="rve-title">${title}</div><button class="rve-close" type="button" aria-label="Закрыть">×</button></div>${inner}</div>`;}
function bindModalChrome(modal,{back=null}={}){modal.querySelector('.rve-close')?.addEventListener('click',closeVocabularyEstimate);if(back)modal.querySelector('.rve-back')?.addEventListener('click',back);}
function currentVocabularyStats(){const root=document.getElementById('reader-chapter-text'),unique=new Set();root?.querySelectorAll('.reader-word[data-word]').forEach(el=>{if(canonicalLang(el.dataset.lang||currentLang())!=='fr')return;const lemma=lemmaForWordSync(el.dataset.word||el.textContent||'');if(lemma)unique.add(lemma);});const profile=loadProfile(),manualMap=manualKnowledgeMapSnapshot();let known=0,unknown=0,unclassified=0;for(const word of unique){const info=classificationForSnapshot(word,profile,manualMap);if(info.value==='known')known+=1;else if(info.value==='unknown')unknown+=1;else unclassified+=1;}return{total:unique.size,known,unknown,unclassified};}
async function openVocabularyDashboard(){if(currentLang()!=='fr')return;const modal=ensureModal();modal.innerHTML=modalShell('<div class="rve-copy">Загружаю французский словарь…</div>',{title:'French vocabulary'});bindModalChrome(modal);try{await loadFrenchData();}catch(error){modal.innerHTML=modalShell(`<div class="rve-copy">Не удалось загрузить данные: ${escapeHtml(error?.message||error)}</div>`,{title:'French vocabulary'});bindModalChrome(modal);return;}const profile=loadProfile(),stats=currentVocabularyStats(),knownPct=stats.total?Math.round(stats.known*100/stats.total):0;modal.innerHTML=modalShell(`<div class="rve-dashboard"><div class="rve-result-kicker">Measure my level</div><div class="rve-number">${profile?`≈ ${formatNumber(profile.estimate)}`:'—'}</div><div class="rve-result-label">${profile?'оценка французского словаря':'уровень ещё не измерен'}</div>${profile?`<div class="rve-known-baseline"><b>${formatNumber(profile.conservativeKnownCount)}</b><span>автоматически Known</span></div>`:''}<div class="rve-stat-grid"><div><b>${stats.total}</b><span>уникальных лемм в главе</span></div><div><b>${stats.known}</b><span>Known · ${knownPct}%</span></div><div><b>${stats.unknown}</b><span>Unknown</span></div></div><div class="rve-rule">Формы объединяются по лемме: <b>suis / est / étaient → être</b>, <b>ai / avait → avoir</b>. Ручное «Знаю / Не знаю» всегда важнее автоматической оценки.</div><button class="rve-primary" type="button">${profile?'Пройти тест заново':'Оценить мой уровень'}</button></div>`,{title:'French vocabulary'});bindModalChrome(modal);modal.querySelector('.rve-primary')?.addEventListener('click',openVocabularyEstimate);}
async function openVocabularyEstimate(){if(currentLang()!=='fr')return;const modal=ensureModal();modal.innerHTML=modalShell('<div class="rve-copy">Загружаю словарь…</div>',{title:'Measure my level'});bindModalChrome(modal);let data;try{data=await loadFrenchData();}catch(error){modal.innerHTML=modalShell(`<div class="rve-copy">Не удалось загрузить данные: ${escapeHtml(error?.message||error)}</div>`,{title:'Measure my level'});bindModalChrome(modal);return;}const profile=loadProfile(),previous=profile?`<div class="rve-rule">Текущая оценка: <b>≈ ${formatNumber(profile.estimate)} слов</b>. Базовыми Known считаются ${formatNumber(profile.conservativeKnownCount)} самых частых. Ручные решения при повторном тесте не стираются.</div>`:'';modal.innerHTML=modalShell(`<div class="rve-welcome"><div class="rve-migachu">W</div><div class="rve-copy rve-copy-center"><b>Короткий тест задаст стартовую оценку французских слов, которые ты уже знаешь.</b><br><br>На каждом экране 14 слов. Отмечай слово только если понимаешь его без подсказки.</div>${previous}<div class="rve-rule">Та же схема, что в English: 42 слова по всему частотному списку, затем ещё 42 около найденной границы. Формы в чтении считаются по лемме.</div><button class="rve-primary" type="button">Начать</button></div>`,{title:'Measure my level'});bindModalChrome(modal);modal.querySelector('.rve-primary')?.addEventListener('click',()=>startAssessment(data));}
function startAssessment(data=frenchData){assessment={phase:1,page:0,step1:selectStep1Indices(data.entries.length,STEP1_COUNT).sort((a,b)=>a-b),step2:[],known1:new Set(),known2:new Set(),preliminaryEstimate:0,startedAt:new Date().toISOString()};renderAssessmentPage();}
function currentIndices(){return assessment?.phase===1?assessment.step1:assessment?.step2||[];}function currentKnownSet(){return assessment?.phase===1?assessment.known1:assessment?.known2||new Set();}function currentPageSlice(){const start=assessment.page*WORDS_PER_PAGE;return currentIndices().slice(start,start+WORDS_PER_PAGE);}function difficultyNumber(){return(assessment?.phase===2?3:0)+Number(assessment?.page||0)+1;}
function renderAssessmentPage(){const modal=ensureModal();if(!assessment||!frenchData)return;const indices=currentPageSlice(),known=currentKnownSet(),selected=indices.filter(i=>known.has(i)).length,chips=indices.map(index=>{const entry=frenchData.entries[index]||{word:'',pos:''},active=known.has(index);return`<button class="rve-word-chip rve-word-chip-fr${active?' is-known':''}" type="button" data-index="${index}" aria-pressed="${active?'true':'false'}"><span>${escapeHtml(entry.word)}</span><i></i></button>`;}).join('');modal.innerHTML=modalShell(`<div class="rve-assessment-head"><div class="rve-page-title">Какие слова ты знаешь?</div><div class="rve-page-desc">Отметь знакомые слова:</div></div><div class="rve-word-grid">${chips}</div><div class="rve-difficulty">Сложность ${difficultyNumber()}</div><div class="rve-wave" aria-hidden="true"></div><div class="rve-known-count">${selected}/${indices.length} знаю</div><button class="rve-primary rve-continue" type="button">Продолжить</button>`,{title:'',back:true});bindModalChrome(modal,{back:goAssessmentBack});modal.querySelectorAll('.rve-word-chip').forEach(button=>button.addEventListener('click',()=>toggleAssessmentWord(Number(button.dataset.index))));modal.querySelector('.rve-continue')?.addEventListener('click',continueAssessment);}
function toggleAssessmentWord(index){if(!assessment||!Number.isInteger(index))return;const known=currentKnownSet();known.has(index)?known.delete(index):known.add(index);renderAssessmentPage();}
function goAssessmentBack(){if(!assessment){closeVocabularyEstimate();return;}if(assessment.page>0){assessment.page-=1;renderAssessmentPage();return;}if(assessment.phase===2){renderIntermediary();return;}openVocabularyEstimate();}
function continueAssessment(){if(!assessment)return;const pageCount=Math.ceil(currentIndices().length/WORDS_PER_PAGE);if(assessment.page<pageCount-1){assessment.page+=1;renderAssessmentPage();return;}if(assessment.phase===1){assessment.preliminaryEstimate=estimateFromChecks(checksFor(assessment.step1,assessment.known1),[...assessment.step1]);renderIntermediary();return;}finishAssessment();}
function renderIntermediary(){const modal=ensureModal();if(!assessment)return;modal.innerHTML=modalShell('<div class="rve-intermediary"><div class="rve-migachu">✓</div><div class="rve-inter-title">Отлично!</div><div class="rve-copy rve-copy-center">Теперь ещё один короткий раунд — слова ближе к предполагаемому уровню.</div><button class="rve-primary" type="button">Продолжить</button></div>',{title:'',back:true});bindModalChrome(modal,{back:()=>{assessment.phase=1;assessment.page=Math.max(0,Math.ceil(assessment.step1.length/WORDS_PER_PAGE)-1);renderAssessmentPage();}});modal.querySelector('.rve-primary')?.addEventListener('click',startAssessmentPhase2);}
function startAssessmentPhase2(){if(!assessment||!frenchData)return;if(!assessment.step2.length)assessment.step2=selectStep2Indices(frenchData.entries.length,assessment.preliminaryEstimate,STEP2_COUNT).sort((a,b)=>a-b);assessment.phase=2;assessment.page=0;renderAssessmentPage();}
function finishAssessment(){if(!assessment||!frenchData)return;const estimate=Math.max(0,Math.round(estimateFromChecks(checksFor(assessment.step2,assessment.known2),[...assessment.step2]))),conservativeKnownCount=conservativeCountForEstimate(estimate,frenchData.entries.length),profile=saveProfile({estimate,conservativeKnownCount,listLength:frenchData.entries.length,source:'wordhoard French v0.1.0 / OpenSubtitles frequency',assessedAt:new Date().toISOString(),step1Estimate:Math.round(assessment.preliminaryEstimate),step1:assessment.step1.map(index=>({index,word:frenchData.entries[index]?.word,known:assessment.known1.has(index)})),step2:assessment.step2.map(index=>({index,word:frenchData.entries[index]?.word,known:assessment.known2.has(index)}))});assessment=null;applyEstimateToRenderedWords().catch(()=>{});Promise.resolve(globalThis.readerSetFrUnknownGlossMode?.('unknown')).catch(()=>{});renderAssessmentResult(profile);}
function renderAssessmentResult(profile){const modal=ensureModal();modal.innerHTML=modalShell(`<div class="rve-result"><div class="rve-result-kicker">Оценка словарного запаса</div><div class="rve-number">≈ ${formatNumber(profile.estimate)}</div><div class="rve-result-label">французских слов</div><div class="rve-known-baseline"><b>${formatNumber(profile.conservativeKnownCount)}</b><span>автоматически Known</span></div><div class="rve-rule">Reader AI не объявляет Known все ≈ ${formatNumber(profile.estimate)} слов. Автоматически Known становятся только ${formatNumber(profile.conservativeKnownCount)} самых частых. Ручные решения имеют приоритет.</div><button class="rve-primary rve-done" type="button">Готово</button><button class="rve-secondary" type="button">Пройти ещё раз</button></div>`,{title:'Measure my level'});bindModalChrome(modal);modal.querySelector('.rve-done')?.addEventListener('click',openVocabularyDashboard);modal.querySelector('.rve-secondary')?.addEventListener('click',()=>startAssessment(frenchData));}
function closeVocabularyEstimate(){document.getElementById(MODAL_ID)?.remove();assessment=null;}
function installExtraStyles(){if(document.getElementById('reader-fr-vocab-style-v1'))return;const style=document.createElement('style');style.id='reader-fr-vocab-style-v1';style.textContent=`#${MODAL_ID} .rve-word-chip-fr{font-family:'IBM Plex Sans',system-ui,sans-serif!important;font-size:1.14rem!important;min-width:92px;max-width:190px}#reader-reading-view .rd-fr-vocab-btn{font-family:'IBM Plex Sans',system-ui,sans-serif;font-weight:800}`;document.head.appendChild(style);}
function queueWordNode(node){if(!(node instanceof Element))return;if(node.classList.contains('reader-word'))pendingWordNodes.add(node);node.querySelectorAll?.('.reader-word').forEach(word=>pendingWordNodes.add(word));}
function flushPendingWordNodes(){pendingBatchScheduled=false;if(pendingWordNodes.size&&currentLang()==='fr'){const batch=Array.from(pendingWordNodes);pendingWordNodes.clear();applyClassificationBatch(batch);}else pendingWordNodes.clear();ensureVocabularyButton();}
function schedulePendingWordBatch(){if(pendingBatchScheduled)return;pendingBatchScheduled=true;requestAnimationFrame(flushPendingWordNodes);}
function installRenderObserver(){if(typeof MutationObserver==='undefined')return;const root=document.getElementById('reader-chapter-text');if(!root){setTimeout(installRenderObserver,250);return;}if(renderObserver&&renderObserverRoot===root)return;renderObserver?.disconnect();renderObserverRoot=root;renderObserver=new MutationObserver(records=>{if(currentLang()!=='fr')return;for(const record of records)for(const node of record.addedNodes||[])queueWordNode(node);if(pendingWordNodes.size)schedulePendingWordBatch();});renderObserver.observe(root,{childList:true,subtree:true});if(currentLang()==='fr'){root.querySelectorAll('.reader-word').forEach(word=>pendingWordNodes.add(word));schedulePendingWordBatch();}}
function ensureVocabularyButton(){const top=document.querySelector('#reader-reading-view .rd-top');if(!top)return null;let button=document.getElementById('reader-fr-vocab-btn');if(!button){button=document.createElement('button');button.id='reader-fr-vocab-btn';button.type='button';button.className='rd-icon rd-fr-vocab-btn';button.textContent='W';button.title='French vocabulary · Measure my level';button.setAttribute('aria-label','French vocabulary and level estimate');button.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();openVocabularyDashboard();});const en=document.getElementById('reader-en-vocab-btn'),zh=document.getElementById('reader-vocab-btn'),pinyin=document.getElementById('reader-pinyin-btn');if(en?.parentNode===top)top.insertBefore(button,en);else if(zh?.parentNode===top)top.insertBefore(button,zh);else if(pinyin?.parentNode===top)top.insertBefore(button,pinyin);else top.appendChild(button);}button.style.display=currentLang()==='fr'?'':'none';return button;}
function installPanelHook(){if(document.documentElement?.dataset?.readerFrVocabPanelHook==='1')return;if(document.documentElement)document.documentElement.dataset.readerFrVocabPanelHook='1';document.addEventListener('click',event=>{if(currentLang()!=='fr')return;const target=event.target instanceof Element?event.target:null;if(!target?.closest?.('#reader-chapter-text .reader-word'))return;queueMicrotask(()=>{decorateWordPanel();syncPanelKnowledge();});setTimeout(()=>{decorateWordPanel();syncPanelKnowledge();},40);},true);}
function installViewObserver(){if(typeof MutationObserver==='undefined')return;const view=document.getElementById('reader-reading-view');if(!view){setTimeout(installViewObserver,250);return;}if(viewObserver&&viewObserverRoot===view)return;viewObserver?.disconnect();viewObserverRoot=view;viewObserver=new MutationObserver(()=>{ensureVocabularyButton();if(currentLang()==='fr'){decorateWordPanel();warmFrenchDataWhenUseful();}});viewObserver.observe(view,{attributes:true,attributeFilter:['data-reader-lang']});}
function warmFrenchDataWhenUseful(){if(currentLang()!=='fr'&&!loadProfile())return;const run=()=>loadFrenchData().then(()=>{if(currentLang()==='fr')return applyEstimateToRenderedWords();}).catch(error=>console.warn('[reader fr vocab] warmup failed',error?.message||error));if(typeof requestIdleCallback==='function')requestIdleCallback(run,{timeout:900});else setTimeout(run,250);}
function boot(){installExtraStyles();ensureVocabularyButton();installPanelHook();installRenderObserver();installViewObserver();if(currentLang()==='fr')decorateWordPanel();warmFrenchDataWhenUseful();}
export function installFrenchVocabularyEstimate(){if(typeof document==='undefined'||typeof document.createElement!=='function')return;if(globalThis.__readerFrenchVocabularyEstimateVersion===1)return;globalThis.__readerFrenchVocabularyEstimateVersion=1;globalThis.readerOpenFrenchVocabularyEstimate=openVocabularyDashboard;globalThis.readerStartFrenchVocabularyEstimate=openVocabularyEstimate;globalThis.readerApplyFrenchVocabularyEstimate=applyEstimateToRenderedWords;globalThis.readerFrenchVocabularyKnowledgeFor=classificationFor;globalThis.readerFrenchLemmaFor=lemmaForWordSync;globalThis.readerFrenchVocabularyProfile=loadProfile;globalThis.readerLoadFrenchVocabularyData=loadFrenchData;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.addEventListener('pageshow',boot);window.addEventListener('reader:fr-vocab-ready',()=>{if(currentLang()==='fr')applyEstimateToRenderedWords().catch(()=>{});});window.addEventListener('an2:languagechange',()=>{ensureVocabularyButton();if(currentLang()==='fr')warmFrenchDataWhenUseful();});}
installFrenchVocabularyEstimate();
export{conservativeCountForEstimate,estimateFromChecks,selectStep1Indices,selectStep2Indices,lemmaForWordSync};
